import styled from 'styled-components';
// ui
import { Box } from '../../ui';
///////////////////////////////////////////////////////////////////////////////////////////////////

export const CloseButton = styled(Box)`
  cursor: pointer;
  transform: scaleX(1.2);
`;

