import styled from 'styled-components';
import {
  top,
  left,
  flex,
  size,
  ratio,
  color,
  space,
  width,
  right,
  height,
  bottom,
  border,
  zIndex,
  opacity,
  display,
  position,
  fontSize,
  flexWrap,
  maxWidth,
  minWidth,
  maxHeight,
  minHeight,
  textAlign,
  boxShadow,
  borderTop,
  alignItems,
  lineHeight,
  fontWeight,
  borderLeft,
  borderColor,
  borderRight,
  borderBottom,
  borderRadius,
  letterSpacing,
  flexDirection,
  justifyContent,
  gridTemplateColumns } from 'styled-system';
import { Box, Flex, Input, PositionedFlex } from '../../ui';

export const InputSymbol = styled(Input)`

`

export const SearchItemBox = styled(PositionedFlex)`
  &:hover {
    background-color: lightblue;
  }
  margin-top: 5px;
`
